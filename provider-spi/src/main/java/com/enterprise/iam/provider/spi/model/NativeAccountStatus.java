package com.enterprise.iam.provider.spi.model;

/** Account status as observed on the target (not the Core governance state). */
public enum NativeAccountStatus { ENABLED, DISABLED, LOCKED, EXPIRED, UNKNOWN }
